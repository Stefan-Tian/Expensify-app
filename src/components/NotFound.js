import React from "react";
import { Link } from "react-router-dom";

const NotFound = () => (
  <div>
    404 not found - <Link to="/">Go back to homepage</Link>
  </div>
);

export default NotFound;
